package models

// for each model this function will works
// TODO: This file maight be deleted
/*
func ReturnMapArr(tags []Tag) []map[string]string {
	tagsResult := make([]map[string]string, len(tags))
	for i, tag := range tags{
		tagsResult[i] = TagReturnMapOne(tag)
	}

	return tagsResult
}
*/
